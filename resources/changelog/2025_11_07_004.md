# Changelog 2025-11-07-004

## Chức năng: Tìm kiếm sản phẩm bằng ảnh (Image-based Product Search)

### Thay đổi

**1. Fix import thiếu trong bot_facebook_messenger.py**
- Thêm `import os` để check file existence

**2. Fix dependency injection cho ProductEnhancedManager**
- Thêm property `product_enhanced_manager` trong `ManagementFactory.__init__.py`
- Auto-inject `knowledge_chunk_manager` khi property được gọi
- Fix tool `search_products_by_image` để dùng manager với dependencies đầy đủ

**3. Fix image download - sử dụng local cache**
- Lưu `_current_sender_id` vào `bot_agent` trong `integration.py`
- Tool `search_products_by_image` ưu tiên load ảnh từ cache local (đã download trong webhook)
- Fallback về download từ URL nếu cache miss
- Giảm lỗi 403 Forbidden khi download từ Facebook CDN

**4. Fix vector search signature mismatch**
- Sửa `search_products_by_image()` dùng `knowledge_chunk_manager.get_all()` thay vì `vector_search()` 
- Tạm thời filter theo metadata (chunk_type, company_id, category, price_range)
- Set default similarity_score = 0.5 (chưa có vector similarity thực sự)

### Ảnh hưởng API

**Search Products by Image Tool** (internal tool - không ảnh hưởng external API)
- Tăng độ tin cậy: Dùng local cache thay vì download lại
- Tăng tốc độ: Load từ file thay vì HTTP request
- Graceful degradation: Friendly error message khi lỗi

### Lưu ý kỹ thuật

**⚠️ TODO - Vector Similarity Search chưa được implement**
- Hiện tại chỉ filter theo metadata, chưa có similarity scoring thực sự
- Cần implement MongoDB Atlas Vector Search hoặc Qdrant integration
- Embedding đã được generate (1024 dims) nhưng chưa được sử dụng để search
- Kết quả hiện tại: Trả về tất cả products có image chunks phù hợp filter

**Roadmap:**
1. Tích hợp Qdrant vector search cho image embeddings
2. Implement similarity scoring với cosine similarity
3. Rank results theo similarity score thực tế
