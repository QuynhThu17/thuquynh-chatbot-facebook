# Changelog 2025-10-09-004

## Cải tiến ProductDescriptionCleaner - Giữ nguyên cấu trúc xuống dòng

### Thay đổi
- Cải tiến class `ProductDescriptionCleaner` để giữ nguyên cấu trúc xuống dòng và định dạng từ HTML gốc
- Chỉ loại bỏ các HTML tags không cần thiết, giữ lại cấu trúc văn bản rõ ràng
- Cải thiện xử lý danh sách với bullet points (•) và số thứ tự
- Tránh trùng lặp nội dung khi làm sạch

### Lợi ích
- Text output giữ được cấu trúc dễ đọc như bản gốc
- Danh sách được format đẹp với bullet points
- Không bị mất thông tin do gộp dòng không phù hợp
- Loại bỏ hiệu quả các HTML tags, URL, phone, email không cần thiết

### Files thay đổi
- `controllers/ultils/product_description_cleaner.py`
  - Thêm method `_clean_html_patterns()` 
  - Thêm method `_preserve_structure()`
  - Thêm method `_format_lists()`
  - Cập nhật method `_final_text_cleanup()`
  - Cải tiến method `clean_description()`