# Changelog - 2025-10-08-005

## 🔧 Fix: API `/super-admin/users/{user_id}` - Extend Subscription Logic

### 🐛 Issues Fixed:
1. **Logic cộng tháng subscription chưa chính xác**
   - Trước: Dùng `timedelta(days=30 * months)` → sai vì không phải tháng nào cũng 30 ngày
   - Sau: Dùng `relativedelta(months=...)` → cộng chính xác theo tháng (30/31 ngày tùy tháng)

2. **Response trả về dữ liệu cũ sau khi update**
   - Trước: Trả về `updated_user` từ lần update đầu tiên (chưa có `package_expires_at` mới)
   - Sau: Fetch lại user từ DB sau khi update subscription/package

### 🔄 Changes:
- **File:** `api/super_user_managerment/api_super_user_managerment.py`
- **Endpoint:** `PATCH /super-admin/users/{user_id}`
- **Parameter:** `extend_subscription_months`

### ✨ Improvements:
1. Import `relativedelta` từ `dateutil.relativedelta` để tính tháng chính xác
2. Thêm update `package_expires_at` cho user sau khi gia hạn subscription
3. Fetch lại user từ DB trước khi trả về response → đảm bảo dữ liệu mới nhất
4. Thêm helper function `initialize_user_data()` để khởi tạo user data

### 📝 Impact:
- Response `user` object sẽ có `package_expires_at` mới nhất sau khi gia hạn
- Subscription được gia hạn chính xác theo tháng (không bị lệch ngày)
